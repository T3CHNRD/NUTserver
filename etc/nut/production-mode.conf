# NUT production mode status/enforcement file.
# Valid modes:
#   off
#   disarmed
#   storm_guard
#   armed
#
# Current enforcement:
#   storm_guard = allows existing approved NUT commit paths
#   disarmed    = blocks live shutdown/orchestration commit actions
#   armed       = reserved until live testing is complete
#   off         = display-only unless nut-monitor.service is actually inactive

NUT_PRODUCTION_MODE=storm_guard
NUT_ALLOW_LIVE_ACTIONS=1
NUT_ALLOW_ESXI_SSH_FALLBACK=0
NUT_MODE_UPDATED_AT="2026-06-11T13:38:28-04:00"
NUT_MODE_UPDATED_BY="nutserver"
NUT_MODE_NOTE="STORM GUARD: existing approved NUT commit paths are allowed; ESXi SSH fallback remains disabled."
