# NUT production mode status file.
# Current version is display-only.
# Later this will become the backend arm/disarm gate.

# Valid modes:
#   off
#   disarmed
#   storm_guard
#   armed

NUT_PRODUCTION_MODE=storm_guard
NUT_ALLOW_LIVE_ACTIONS=1
NUT_ALLOW_ESXI_SSH_FALLBACK=0
NUT_MODE_NOTE="Storm Guard: NUT monitoring is active. Existing UPS timer/action path remains in place. ESXi SSH fallback remains disabled until approved live test."
