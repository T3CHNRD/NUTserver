# TEMPLATE FILE - SANITIZED
# Copy this file to /etc/nut/upsd.users on the target NUT server.
# Replace every CHANGE_ME value locally.
# Do not commit live usernames, passwords, tokens, or production-only secrets.

[CHANGE_ME_MONITOR_USER]
    password = CHANGE_ME_PASSWORD
    upsmon master

[CHANGE_ME_ADMIN_USER]
    password = CHANGE_ME_PASSWORD
    actions = SET
    instcmds = ALL
